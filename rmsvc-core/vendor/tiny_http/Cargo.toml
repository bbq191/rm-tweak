# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
# 2026-09-24 从 crates.io tiny_http 0.12.0 原样复制，只加了"每条连接读空闲超时""accept 暂时性错误不退出"两处改动，见 rmsvc-core/vendor/README.md。
edition = "2018"
name = "tiny_http"
version = "0.12.0"
authors = [
    "pierre.krieger1708@gmail.com",
    "Corey Farwell <coreyf@rwell.org>",
]
description = "Low level HTTP server library"
documentation = "https://tiny-http.github.io/tiny-http/tiny_http/index.html"
readme = "README.md"
keywords = [
    "http",
    "server",
    "web",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/tiny-http/tiny-http"

[package.metadata.docs.rs]
features = ["ssl-openssl"]

[dependencies.ascii]
version = "1.0"

[dependencies.chunked_transfer]
version = "1"

[dependencies.httpdate]
version = "1.0.2"

[dependencies.log]
version = "0.4.4"

[dependencies.openssl]
version = "0.10"
optional = true

[dependencies.rustls]
version = "0.20"
optional = true

[dependencies.rustls-pemfile]
version = "0.2.1"
optional = true

[dependencies.zeroize]
version = "1"
optional = true




[features]
default = []
ssl = ["ssl-openssl"]
ssl-openssl = [
    "openssl",
    "zeroize",
]
ssl-rustls = [
    "rustls",
    "rustls-pemfile",
    "zeroize",
]

# 第三方原样代码：本仓库不改它的风格，编译/clippy 告警一律静音（只看我们自己的代码零告警）。
[lints.rust]
warnings = "allow"

[lints.clippy]
all = "allow"
