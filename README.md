# cang-jie

**[English](docs/README.en.md)**

reMarkable Paper Pro Move 的设备增强套件。**不修改 xochitl**（设备自带的阅读/笔记应用）本体，
而是用两样东西给它补功能：[xovi](https://github.com/asivery/xovi)（第三方扩展加载框架）加载的小插件，
和一组跑在设备上的网页服务。补的是书籍管理、笔记整理和一些系统层面的小改进。

## 能做什么

| 你想要 | 对应的功能 | 在哪 |
|---|---|---|
| 2026-09-25 | 回收站 / 建文件夹的设备端代理试满 5 次仍不成功时，网页页头横幅提示（点「知道了」清掉）；勾画旁边后补手写、或擦掉手写，笔记仍是原来那条，已校对的状态和文字保留 |
| 2026-09-25 | **第四轮全系统审计**（修了 50 多处问题）：笔记条目在"擦除后撤销"或书从回收站恢复时会复活，已校对的文字不丢；`.metadata` 读一半不再被当成"书被删了"；超限拆分漫画不再丢封面和扉页；scp 传到一半的书不再被当成整本入库；书名和目录里的 `&` 不再显示成 `&amp;`；服务遇到暂时性的连接错误不再悄悄停掉；损坏的 `.rm` 或 PDF 不再让服务崩溃。省电方面：字体服务开机不再全量重扫，界面代理出错后逐步拉长重试间隔（15→120 秒），上传暂存从内存盘移到 /home。安装脚本减少了 ssh 往返，整机重启失败时如实报错。本轮改动只在开发机上测过（模拟测试 314 项），还没上真机 |
| 把书弄进设备，而且在墨水屏上好读 | 网页上传或抓网文，书先进"母版库"。EPUB 可一键"优化"（排版、目录、封面、脚注）；有文字层的 PDF 按原格式转成 EPUB。然后加入原生阅读器或 KOReader（KOReader 预置文字书 / 漫画两套阅读方案）。超过 xochitl 约 100MB 上传上限的大书也能进，漫画有专门处理；母版库里的书可以下载原件、改名、按书设阅读方向 | [`shelf/`](shelf/) |
| 把荧光笔勾画和旁边的手写批注变成能整理的笔记 | 合上书自动收进条目库。在手机网页上校对手写转写、全文搜索、选 AI 模型提问，再投回设备笔记本（标题、列表、复选框用设备自带样式）或导出 Obsidian markdown | [`notes/`](notes/) |
| 系统层面的小改进 | 荧光笔划中文"划哪吸哪"、手写笔画粗细优化、xochitl 阅读器单击翻页与日漫翻页规则、电池耗电诊断、字体和壁纸上传即用 | [`enhance/`](enhance/) |
| 在手机或电脑上统一操作以上功能 | 一个 HTTPS 网页入口，带登录密码，把请求转给各个服务；另有设备健康页（服务状态、扩展是否加载、开机耗时、上次开机日志） | [`gateway/`](gateway/) |
| 新设备一条命令装完 | 带固件兼容性校验，也能一条命令卸载；部署后自动核对设备状态 | [`packaging/`](packaging/) |

另有一个"幕后"目录：[`rmsvc-core/`](rmsvc-core/)（各网页服务共用的基础库，不含业务逻辑）。

所有功能都跑在设备的**官方系统**上，不重新打包 xochitl。

## 近期更新

| 日期 | 新增 |
|---|---|
| 2026-09-25 | **让改动生效改成整机重启**（约 20–60 秒回来），不再单独重启 xochitl：实测停止 xochitl 本身就有概率在退出途中崩溃（它自己的退出顺序问题，与扩展无关），进而触发应急整机重启；现在改为主动干净重启，安装器会等设备回来并自动跑 `packaging/verify-on-device.sh` 只读核对（9 类 44 项）。手动重启设备也会把待换入的新版扩展换进去 |
| 2026-09-25 | 管理页新增「设备健康」（xochitl/xovi 状态、已加载扩展、各服务重启次数与内存峰值、开机耗时、上次开机最后日志、清理遗留数据）；固件升级后网页顶部提示需要重新安装；「移到 xochitl 回收站」改由设备端常驻代理执行，几秒内生效 |
| 2026-09-25 | 母版库可按书设阅读方向（自动 / 从右往左 / 从左往右，可批量），写进书里并同步到日漫翻页规则；笔记的 `##` / `###` / `1.` / `-` / `- [ ]` 标记与设备自带文字样式一一对应，生成的笔记带书的小节名；手写批注配对距离放宽 |
| 2026-09-24 | 第三轮全系统审计：多处省电（建文件夹长轮询 25→290 秒、壁纸服务改监听文件读取、局域网名字服务只在 IP 变化时唤醒）、大书不再让笔记服务内存超限、所有服务加连接读空闲超时、私有 CA 加名称约束（**升级后需在每台手机/电脑重新安装证书**，见 [INSTALL.md](docs/INSTALL.md#装完之后)） |
| 2026-09-24 | xochitl 阅读器「单击翻页」与「日漫翻页规则」（管理 → 系统增强，默认关）；KOReader 预置文字书 / 漫画两套方案；笔记跨书全文搜索；母版库下载原件、改名；PDF 转 EPUB 后原件保留 7 天可恢复；9 个后台服务开机不再等网络 |
| 2026-09-23 | 有文字层的 PDF 按原格式转 EPUB（颜色、图片位置与比例、链接）；EPUB 优化多处修复并新增两条质量检查；笔记页新增「导入 KOReader 批注」按钮；电池诊断常驻进程不再派生子进程 |
| 2026-09-22 | 漫画/PDF 拆分投递改真正流式，修复大部头合集的真机内存尖峰风险（一处实测从 233MB 降到 93MB）；图片瘦身安全阈值改用真机实测重新标定，此前按公式估算的值偏低、形同虚设 |
| 2026-09-22 | 安装/卸载脚本大改：装前预检（ssh 连通/固件白名单/设备剩余空间）、按需重启（这轮没有改动就不再重启 xochitl，不再每次都闪屏）、`--dry-run` 可预演不碰设备、卸载改逆序执行并清理自己留下的载荷目录 |
| 2026-09-22 | appload 3.28 兼容补丁工具整体退役——上游 v0.6.0 已原生支持 3.28/3.29，改走 `vellum upgrade appload` 即可 |
| 2026-09-22 | 母版库页面体验修复：加入书库的文件夹选择改读真实设备文件夹列表；建文件夹网页代理改长轮询 |
| 2026-09-22 | 笔记线新增 KOReader 高亮/生词回流摄取，与 xochitl 原生阅读器的批注管线并行工作 |

## 快速开始

**只支持 reMarkable Paper Pro Move、固件 3.28.0.172**。这是目前唯一验证过的版本，安装脚本会核对，不符默认拒装。

1. 在设备上手动装好四样基础组件：`vellum add xovi`、`vellum add qt-resource-rebuilder`、`vellum add appload`（≥ 0.6.0），再经 appload 侧载 KOReader。它们不属于本项目，安装脚本不代装。
2. 电脑上要有 Rust 交叉编译环境，并能免密 ssh 到设备的 root（清单见 [INSTALL.md「装之前」](docs/INSTALL.md#装之前)）。用 USB 线连上设备，然后：

   ```sh
   git clone https://github.com/bbq191/rm-tweak.git
   cd rm-tweak/packaging
   sh install-all.sh --dry-run        # 先预演：只打印计划，不连设备
   sh install-all.sh 10.11.99.1
   ```
   最后一步会整机重启设备一次（约 1 分钟），回来后脚本自动核对，逐项打 ✓/⚠/✗。
3. 浏览器打开 `https://10.11.99.1/`，默认密码 `shelf`，第一次登录必须改密码。

卸载：`sh uninstall-all.sh 10.11.99.1`。前置条件、风险、固件升级（OTA）后怎么恢复，全部在 **[docs/INSTALL.md](docs/INSTALL.md)**。

## 历史与范围

这个项目最早从"reMarkable 中文输入法"起步（内部开发代号 `cang-jie` 即"仓颉"，中国传说中造字的
人——本页标题沿用这个名字，但公开发行的这份仓库改叫更贴切的
[`rm-tweak`](https://github.com/bbq191/rm-tweak)），后来陆续长出了阅读增强、PKM 知识管理、
手写识别等更多方向。
2026-09-11 做过一次大规模仓库整理：中文输入法、完整阅读管线、PKM/手写识别这几条较早期的功能线，
连同它们的逆向基座代码，整体移出了内部开发仓库（不是删除，是搬到维护者本机另一个不随 git 走的
目录）——**这些功能目前在设备上仍是现役、仍在正常运行**，只是源码不再随这份公开代码分发，也不再
有活跃开发。当前公开的是上表这几条线，走的是比早期更保守的架构（独立 Web 服务 + 尽量小的 xovi
扩展面），跟设备官方系统的耦合程度比早期低很多。

## 打赏

如果这个项目帮到了你，欢迎请作者喝杯咖啡——纯自愿，打不打赏都不影响任何功能的使用。收款码、
金额使用说明等详见独立页面 **[DONATE.md](docs/DONATE.md)**。

## 协议 / 免责

本仓库代码以 **[Apache License 2.0](LICENSE)** 开源，可自由使用/修改/分发（含商用），需保留
版权声明；协议本身附带专利授权条款。原本作为个人自用工具开发，源码按现状（as-is）提供，不附带
任何担保。

涉及第三方许可证的地方（词典数据、字体等，如有引入）以代码内注释的实测记录为准，不构成法律
意见。跟 reMarkable、[xovi](https://github.com/asivery/xovi)、vellum、KOReader 等第三方项目/
商标没有从属关系，均为其各自权利人独立的第三方项目/商标。
