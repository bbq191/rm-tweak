# cang-jie 固件白名单 —— 每行：<sha256(/usr/bin/xochitl)>  <人读标签>
#
# 【为什么用 sha256 而非版本字符串】设备端 install.sh 的"固件门"逐字节比对
# /usr/bin/xochitl 的 sha256（rmtool fail-safe 范式，见中文化白皮书 §4.3）。
# 比 /etc/version 版本号更严：qmldiff 注入靠 xochitl 的 QML 资源布局，同一版本号
# 的热修都可能挪动布局；哈希对上才保证"本包在这颗二进制上验证过、注入定位可靠"。
#
# 【新老设备通用】同一固件的任意设备，其 /usr/bin/xochitl 逐字节相同 → sha256 相同
# → 白名单命中 → 一键装。装错固件（哈希不在表内）默认拒绝，避免 qmd 定位错位崩溃；
# 确要装用 `install.sh --force`（会自动把当前哈希追加进本表，并交给设备端多层
# fail-safe 兜底：.so 内部特征码自定位 + qrr 崩溃自愈 + ExecStartPre 摘链裸启）。
#
# 【维护】OTA 到新固件、真机验证注入无误后，把新哈希加一行即可让本包认它为"版本对"。
# 下面这行的哈希 = rmfw/xochitl_3.28.0.169.bin 提取件，已于 2026-08-28 与设备实测
#   `sha256sum /usr/bin/xochitl` **逐字节一致核对通过**（machine=reMarkable Chiappa，
#   /etc/version=20260806095513）。提取件与在盘文件同源，固件门可直接命中。
#
# 【2026-09-11 追记】本文件随那次全仓库归档挪出过仓库、又原样搬回来——门本身现在挂在
# **host 侧** `packaging/install-all.sh`（ssh 拉 sha256 比对这张表），不是"设备端
# install.sh 的固件门"（`shelf/install.sh` 本身没有这层检查，只有 dm-verity 门 + 按
# `IMG_VERSION` 选字体菜单 qmd 版本这种适配逻辑，跟这里的安全拒装语义不是一回事）。
#
# 【2026-09-11 用户纠正】3.28.0.169 那一行是搬回来时没核对就原样带回的——那是 2026-08-28
# 记的哈希，设备早在 2026-09-05 就 OTA 到了 3.28.0.169 → 3.28.0.172（shelf 白皮书 §03v），
# 当前实际在用的设备固件是 3.28.0.172，那一行哈希对不上。保留只作历史记录（万一有设备还
# 停留在 .169），不代表当前设备。3.28.0.172 那一行是用户当场 `ssh root@10.11.99.1
# sha256sum /usr/bin/xochitl` 现取、报给我加进来的——真实设备实测值，不是推算/凭记忆写的。
# 6361610111c381ce730a8bfcc889bd933ef5fef173563a9156e435233714e7ee  3.28.0.169 (imx93-chiappa / Paper Pro Move，历史记录，非当前设备固件)
5ba79d1b5656df1a771217d29a8d3938c40256be53361b10a0d17cd4752807f4  3.28.0.172 (imx93-chiappa / Paper Pro Move，2026-09-11 用户设备实测 sha256sum /usr/bin/xochitl 核对)
